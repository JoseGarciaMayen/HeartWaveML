[pytest]
testpaths = tests
markers =
    integration: tests that load the real models (require `dvc pull`)
filterwarnings =
    ignore:`np.bool8` is a deprecated alias.*:DeprecationWarning
    ignore:non-integer arguments to randrange.*:DeprecationWarning
